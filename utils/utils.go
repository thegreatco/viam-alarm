package utils

var Version = "v0.0.1"

// Change the namespace to match your organization name so it can be uploaded to your private registry
var Namespace = "thegreatco"

// Change the logger name if you want to customize it
var LoggerName = "viam-alarms"
